package plumbing

// BuildTreeFromIndex (entries []IndexEntry) *TreeNode

// WriteTree(node *TreeNode) ([20]byte, error)

// FlattenTree(node *TreeNode) []TreeEntry

// used by commit, ls-tree, cat-file -p (tree)

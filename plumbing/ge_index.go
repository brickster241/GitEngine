package plumbing

// LoadIndex() ([]IndexEntry, error)

// WriteIndex(entries []IndexEntry) error

// UpsertIndexEntry(entries []IndexEntry, entry IndexEntry)

// RemoveIndexEntry(entries []IndexEntry, entry IndexEntry)

// Used by add, status, commit
